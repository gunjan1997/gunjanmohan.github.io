# Gunjan Mohan — Data Engineer (Dark Portfolio)
React + Vite + Framer Motion. No Tailwind. Ready for GitHub Pages.

## Quick start
npm install
npm run dev

## Build
npm run build

## Deploy (existing flow)
git add .
git commit -m "Update"
git push origin main
git subtree push --prefix dist origin gh-pages
